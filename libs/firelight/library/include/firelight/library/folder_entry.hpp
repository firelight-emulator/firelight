#pragma once

namespace firelight::library {

/**
* Represents an entry's membership in a manual folder
*/
struct FolderEntry {
  int folderId = -1;
  int entryId = -1;
};
} // namespace firelight::library
