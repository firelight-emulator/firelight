import QtQuick

// From the catalog's `notifications` page. These persist and are searchable, but
// nothing reads them yet — see the `_note` on each entry in the catalog.
SettingsPage {
    page: "notifications"
}
