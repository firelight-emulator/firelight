# Built test binaries (regenerable via build.sh)
*.exe
*.o
*.obj
# Generated movie artifacts
*.fltm
# ...but keep the checked-in Pikachu example: the Pallet anchor savestate is required
# input to recompile the script, and the result movie is the verified deliverable.
!examples/yellow_pallet_anchor.fltm
!examples/yellow_get_pikachu.fltm
!examples/yellow_get_pikachu_from_boot.fltm
# ...and the CI determinism-gate reference movie (checkpoint-free; over tests/testrom.gb).
!tests/reference.fltm
